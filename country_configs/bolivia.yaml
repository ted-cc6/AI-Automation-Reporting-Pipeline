country: bolivia
label: Bolivia
report_context: >
  Part of VisionFund's LARCO (Latin America and Caribbean) insurance survey,
  data_loader_larco/ schema. First-pass config -- no Bolivia-specific
  segment/metric nuances confirmed yet; only the structural gap that applies
  to every LARCO country (no disability screener question) is recorded
  below. Update this file once country-specific context is available.

segment_overrides:
  pwd:
    available: false
    skip_reason: >
      LARCO's survey instrument has no Washington Group disability screener
      question (data_loader_larco/column_mapping.csv has no q_disability
      row) -- this would already resolve to an empty/skipped segment via
      analysis_engine/segments.py's required-column check even without this
      override; recorded explicitly here for visibility.
