# Runtime + dev dependencies are pinned in pyproject.toml (this is now the
# source of truth). This file exists only so `pip install -r requirements.txt`
# still works as a convenience entry point (e.g. in CI).
-e .[dev]
